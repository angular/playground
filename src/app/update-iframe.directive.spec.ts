import { UpdateIframeDirective } from './update-iframe.directive';

describe('UpdateIframeDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateIframeDirective();
    expect(directive).toBeTruthy();
  });
});
