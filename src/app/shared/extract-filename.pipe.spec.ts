import { ExtractFilenamePipe } from './extract-filename.pipe';

describe('ExtractFilenamePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractFilenamePipe();
    expect(pipe).toBeTruthy();
  });
});
